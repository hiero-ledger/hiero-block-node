# Claude Code Guidelines for hiero-block-node

## Code Formatting

Always run `./gradlew spotlessApply` after making code changes to ensure consistent formatting before committing.

```bash
./gradlew spotlessApply
```
