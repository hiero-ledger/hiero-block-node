# SPDX-License-Identifier: Apache-2.0
# Solo Scripts Environment Configuration
#
# Copy this file to .env and customize as needed.
# All values have sensible defaults if not specified.

# =============================================================================
# Topology Configuration
# =============================================================================

# Network topology to deploy
# Available topologies (see: task topologies):
#   - single           : 1 Consensus Node, 1 Block Node (default)
#   - paired-3         : 3 Consensus Nodes, 3 Block Nodes
#   - fan-out-3cn-2bn  : 3 Consensus Nodes, 2 Block Nodes
TOPOLOGY=single

# =============================================================================
# Cluster Configuration
# =============================================================================

# Kind cluster name
CLUSTER_NAME=solo-cluster

# Kubernetes namespace for Solo deployment
NAMESPACE=solo-network
# Solo deployment name
DEPLOYMENT=deployment-solo

# =============================================================================
# Version Configuration
# =============================================================================

# Consensus Node version
# Options: 'latest' (latest GA), or specific tag (e.g., 'v0.68.6')
CN_VERSION=latest

# Mirror Node version
# Options: 'latest' (latest GA), or specific tag (e.g., 'v0.146.0')
MN_VERSION=latest

# Block Node version
# Options: 'latest' (latest GA), 'main' (current snapshot), or specific tag (e.g., 'v0.21.2')
BN_VERSION=latest

# Solo CLI version
# Options: 'latest' or specific version (e.g., '0.52.0')
SOLO_VERSION=latest
