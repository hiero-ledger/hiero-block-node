# SPDX-License-Identifier: Apache-2.0
# Solo E2E Test Environment - Copy to .env and customize

# Topology: single (default), paired-3, fan-out-3cn-2bn (see: task topologies)
TOPOLOGY=single

# Cluster
CLUSTER_NAME=solo-cluster
NAMESPACE=solo-network
DEPLOYMENT=deployment-solo

# Versions: 'latest', 'main' (BN only), or specific tag (e.g., 'v0.68.6')
CN_VERSION=latest
MN_VERSION=latest
BN_VERSION=latest
SOLO_VERSION=latest

# NLG Load Generation (direct parameters for task load:up)
NLG_TEST_CLASS=CryptoTransferLoadTest
NLG_CONCURRENCY=5    # -c: concurrent threads
NLG_ACCOUNTS=10      # -a: number of accounts
NLG_DURATION=300     # -t: duration in seconds
NLG_EXTRA_ARGS=      # Extra args, e.g., "-T 5 -K ED25519"

# Solo environment variables (passed through to Solo CLI)
MIRROR_NODE_PINGER_TPS=5  # Mirror Node pinger TPS (0 to disable)

# Local Metrics (Prometheus+Grafana stack on last block-node)
ENABLE_LOCAL_METRICS=false

# Test Definition (for task test:run): none, smoke-test.yaml, basic-load.yaml, node-restart-resilience.yaml
TEST_FILE=none
